# Packages used by the independent analysis layer beyond the original model runtime.
pyarrow>=14
PyYAML>=6
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Optional third-priority attribution support. The workflow does not fabricate
# SHAP output when this dependency or a compatible analysis path is unavailable.
shap>=0.45
