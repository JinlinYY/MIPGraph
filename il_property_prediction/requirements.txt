# Core numerical stack
numpy>=1.24,<2.0
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10

# Deep learning
# Install torch and torch-geometric wheels that match your CUDA/Python version.
# The unpinned entries below are suitable for CPU or environments where pip can
# resolve compatible wheels automatically. For CUDA builds, follow:
# https://pytorch.org/get-started/locally/
# https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
torch>=2.0
torch-geometric>=2.4

# Cheminformatics
rdkit>=2023.9

# Data files and configuration
openpyxl>=3.1
PyYAML>=6.0
tqdm>=4.66

# Visualization
matplotlib>=3.7
Pillow>=9.0

# Optional but useful for baselines / saved artifacts
joblib>=1.3
